templates:
- condition: template == 1
  spans:
  template: |
    Test-2 {{ "abc" | annotate("2") }} Test-2

- condition: template == 2
  spans:
  - label:
  template: |
    Test-2 {{ "abc" | annotate("2") }} Test-2

- condition: template == 3
  spans:
  - label:
    pattern:
  template: |
    Test-2 {{ "abc" | annotate("2") }} Test-2

- condition: template == 4
  spans:
  - label: X
    pattern: \b.*?\b
  template: |
    Test-1 {{ "abc" | annotate("1") }}
    Test-2 {{ "def" | annotate("2") }}
    Test-3 {{ "ghi" | annotate("3") }}

- condition: template == 5
  spans:
    - label: X
      pattern: "{$1}.*?{$2}"
    - label: Y
      pattern: "{$1}.*?{$3}"
    - label: Z
      pattern: "{$2}.*?{$3}"
    - label: A
      pattern: "{$0}.*?{$3}"
  template: |
    Test-1 {{ "abc" | annotate("1") }}
    Test-2 {{ "def" | annotate("2") }}
    Test-3 {{ "ghi" | annotate("3") }}
